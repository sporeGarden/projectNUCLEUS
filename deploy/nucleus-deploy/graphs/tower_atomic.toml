# Tower Atomic — security + discovery + defense
[graph]
id = "tower_atomic"
version = "1.0.0"
description = "Tower Atomic composition — beardog, songbird, skunkbat"

[[nodes]]
id = "beardog"
binary = "beardog"
tier = "tower"
required = true

[[nodes]]
id = "songbird"
binary = "songbird"
tier = "tower"
depends_on = ["beardog"]
required = true

[[nodes]]
id = "skunkbat"
binary = "skunkbat"
tier = "tower"
depends_on = ["beardog"]
