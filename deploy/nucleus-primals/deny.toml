# SPDX-License-Identifier: AGPL-3.0-or-later
# cargo-deny configuration — nucleus-primals
# Zero-dependency registry crate. Pure Rust, no external deps.

[advisories]
yanked = "deny"
unmaintained = "workspace"

[licenses]
allow = [
    "AGPL-3.0-or-later",
    "CDLA-Permissive-2.0",
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "Unicode-DFS-2016",
    "Zlib",
]

[bans]
multiple-versions = "warn"
wildcards = "deny"
deny = [
    { crate = "openssl", reason = "No OpenSSL in ecoPrimals" },
    { crate = "openssl-sys", reason = "No C bindings" },
    { crate = "native-tls", reason = "No platform TLS" },
    { crate = "unsafe-libyaml", reason = "C YAML parser — use serde-saphyr (pure Rust)" },
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
