# Gate-level environment for systemd units.
# Installed to /etc/projectnucleus/gate.env by gate_provision.sh.
# Systemd units reference these via EnvironmentFile directive.
# gate_provision.sh replaces this default with the actual gate user's home.
GATE_HOME=${GATE_HOME:-/home/$(whoami)}
